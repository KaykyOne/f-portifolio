---
import { Image } from "astro:assets";

const {
  src,
  alt = "",
  className = "",
  width = 100,
  height = 100,
} = Astro.props;

const finalSrc = `/${src}`;
---

<Image
  src={finalSrc}
  alt={alt}
  class={className}
  width={width}
  height={height}
/>